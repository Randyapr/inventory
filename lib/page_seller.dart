import 'package:flutter/material.dart';

class PageSeller extends StatelessWidget {
  const PageSeller({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        'Halaman Seller',
        style: TextStyle(fontSize: 24),
      ),
    );
  }
}
