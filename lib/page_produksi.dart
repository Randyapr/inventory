import 'package:flutter/material.dart';

class PageProduksi extends StatelessWidget {
  const PageProduksi({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        'Halaman Produksi',
        style: TextStyle(fontSize: 24),
      ),
    );
  }
}
